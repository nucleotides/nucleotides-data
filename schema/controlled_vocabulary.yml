---
type: seq
sequence:
  - type: map
    mapping:
      name:
        type: str
        required: true
        unique: true
        pattern: /^[a-z0-9_.]*$/
      desc:
        type: str
        required: true
