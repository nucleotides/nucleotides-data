---
machine:
  ruby:
    version: 2.2.3
  services:
    - docker

dependencies:
  override:
    - ./script/bootstrap

test:
  override:
    - ./script/test

deployment:
  hub:
    branch: master
    commands:
      - make deploy
